---
phase: 04-quality-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/quest/dashboard/page.tsx
  - app/quest/session/[id]/page.tsx
  - components/quest/reveal-sequence.tsx
  - hooks/use-scores.ts
  - hooks/__tests__/use-quest-state.test.ts
  - hooks/__tests__/use-scores.test.ts
  - lib/validation/__tests__/error-classification.test.ts
  - lib/scoring/__tests__/riasec.test.ts
  - lib/scoring/__tests__/mbti.test.ts
  - lib/scoring/__tests__/mi.test.ts
  - lib/scoring/__tests__/values.test.ts
  - lib/scoring/__tests__/strengths.test.ts
  - lib/scoring/__tests__/nan-guard.test.ts
autonomous: true
requirements:
  - AUDIT-01
  - AUDIT-02

must_haves:
  truths:
    - "ESLint reports zero warnings and zero errors on the main codebase"
    - "TypeScript compiles with no errors"
    - "All scoring boundary values (0, 100, -100, threshold) have explicit test coverage"
    - "All existing 840+ tests still pass"
  artifacts:
    - path: "app/quest/dashboard/page.tsx"
      provides: "Cleaned dashboard page — unused ClassLabel import removed, <a> replaced with <Link>"
    - path: "hooks/use-scores.ts"
      provides: "Cleaned hook — unused imports removed"
    - path: "components/quest/reveal-sequence.tsx"
      provides: "Cleaned reveal — handlePostConfirmatory dead code removed, useCallback dep fixed"
    - path: "lib/scoring/__tests__/riasec.test.ts"
      provides: "Boundary tests for calculateAllRiasec, deriveClassLabel at 50/51"
    - path: "lib/scoring/__tests__/nan-guard.test.ts"
      provides: "Boundary tests for calculateAll* with extreme inputs"
  key_links:
    - from: "hooks/use-scores.ts"
      to: "lib/scoring/*.ts"
      via: "import statements"
      pattern: "import.*from.*@/lib/scoring"
---

<objective>
Fix all ESLint warnings/errors and fill scoring test boundary gaps.

Purpose: AUDIT-01 requires zero lint issues and clean conventions. AUDIT-02 requires scoring edge case coverage at boundary values. Phase 3 left 840 tests passing but specific boundary values (score 0, 100, -100, threshold 35, deriveClassLabel at 50/51, all-null ipsatives) are untested.

Output: Zero-warning lint pass, clean TypeScript compilation, boundary-value scoring tests added.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-quality-audit/04-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix all ESLint warnings and dead code (AUDIT-01)</name>
  <files>
    app/quest/dashboard/page.tsx,
    app/quest/session/[id]/page.tsx,
    components/quest/reveal-sequence.tsx,
    hooks/use-scores.ts,
    hooks/__tests__/use-quest-state.test.ts,
    hooks/__tests__/use-scores.test.ts,
    lib/validation/__tests__/error-classification.test.ts
  </files>
  <read_first>
    app/quest/dashboard/page.tsx,
    app/quest/session/[id]/page.tsx,
    components/quest/reveal-sequence.tsx,
    hooks/use-scores.ts,
    hooks/__tests__/use-quest-state.test.ts,
    hooks/__tests__/use-scores.test.ts,
    lib/validation/__tests__/error-classification.test.ts
  </read_first>
  <action>
    Fix every ESLint warning and error in the main codebase per D-01 priority. The exact issues to fix:

    **app/quest/dashboard/page.tsx:**
    1. Line 9: Remove the unused `ClassLabel` import (it is not used in JSX — `EmergingType` and `deriveClassLabel` handle class display)
    2. Line 144: Replace the `<a href="/">` element with `<Link href="/">` from `next/link`. Add `import Link from "next/link"` to the imports.

    **app/quest/session/[id]/page.tsx:**
    1. Line 397: The parameter `_data` in `handleSelfMapComplete` callback is already prefixed with underscore, but ESLint still flags it. Change the parameter destructuring to use `_` as the parameter name: `(_: { clarity: number; sources: string[]; perceived_strengths: string[] })`.

    **components/quest/reveal-sequence.tsx:**
    1. Line 116: Remove the entire `handlePostConfirmatory` useCallback (lines 116-119) — it is dead code, assigned but never used.
    2. Line 113: Fix the `react-hooks/exhaustive-deps` warning on the `handleAdvance` useCallback. The dependency `onSessionComplete` is flagged as unnecessary. Read the full useCallback body to determine if `onSessionComplete` is actually used inside it. If it IS used inside the callback body, the warning is a false positive — add `// eslint-disable-next-line react-hooks/exhaustive-deps` above the dependency array. If it is NOT used inside the callback body, remove it from the dependency array.

    **hooks/use-scores.ts:**
    1. Line 6: Remove `calculateRiasecType` from the destructured import of `@/lib/scoring/riasec`
    2. Line 12: Remove `getTopMi` from the import of `@/lib/scoring/mi`
    3. Line 15: Remove `deriveEmergingType` from the import of `@/lib/scoring/mbti`
    IMPORTANT per Pitfall 4: These are unused imports IN THIS FILE. The functions themselves are used elsewhere. Only remove the import bindings, not the exports from scoring modules.

    **hooks/__tests__/use-quest-state.test.ts:**
    1. Line 6: Remove unused `FlowPhase` import

    **hooks/__tests__/use-scores.test.ts:**
    1. Line 8: Remove unused `ResponseSignalFootprint` import

    **lib/validation/__tests__/error-classification.test.ts:**
    1. Line 2: Remove unused `ErrorCategory` import
  </action>
  <verify>
    <automated>cd "/Users/gerrygan/Career Quest" && npx eslint app/ components/ hooks/ lib/ --max-warnings 0 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `npx eslint app/ components/ hooks/ lib/ --max-warnings 0` exits with code 0
    - `npx tsc --noEmit` exits with code 0
    - `npx vitest run` shows all tests passing (840+)
    - grep for `ClassLabel` in dashboard/page.tsx returns zero matches
    - grep for `handlePostConfirmatory` in reveal-sequence.tsx returns zero matches
    - grep for `calculateRiasecType` in hooks/use-scores.ts returns zero matches
  </acceptance_criteria>
  <done>Zero ESLint warnings/errors across all main codebase files. All dead code removed. TypeScript compiles clean. All existing tests still pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add scoring boundary-value tests (AUDIT-02)</name>
  <files>
    lib/scoring/__tests__/riasec.test.ts,
    lib/scoring/__tests__/mbti.test.ts,
    lib/scoring/__tests__/mi.test.ts,
    lib/scoring/__tests__/values.test.ts,
    lib/scoring/__tests__/strengths.test.ts,
    lib/scoring/__tests__/nan-guard.test.ts
  </files>
  <read_first>
    lib/scoring/__tests__/riasec.test.ts,
    lib/scoring/__tests__/mbti.test.ts,
    lib/scoring/__tests__/mi.test.ts,
    lib/scoring/__tests__/values.test.ts,
    lib/scoring/__tests__/strengths.test.ts,
    lib/scoring/__tests__/nan-guard.test.ts,
    lib/scoring/riasec.ts,
    lib/scoring/mbti.ts,
    lib/scoring/mi.ts,
    lib/scoring/values.ts,
    lib/scoring/strengths.ts
  </read_first>
  <behavior>
    Add a "Boundary values" describe block to each test file. Specific cases per D-07:

    **riasec.test.ts:**
    - `calculateAllRiasec` with all-max responses (every RIASEC type gets maximum score) — scores should be 100 for all types
    - `calculateAllRiasec` with all-min responses (every type gets minimum score) — scores should be 0 for all types
    - `deriveClassLabel` with a score exactly at 50 — function uses `> 50` (strict), so 50 should NOT be above threshold. Test with `{ R: 50, I: 50, A: 0, S: 0, E: 0, C: 0 }` — neither R nor I qualifies
    - `deriveClassLabel` with a score at 51 — should qualify as primary type
    - `mergeIpsativeScores` with all-null ipsative values — should return base scores unchanged

    **mbti.test.ts:**
    - `deriveEmergingType` with scores exactly at threshold 35 — `isStillEmerging(35)` is false, so 35 is NOT emerging. Verify the full type string with all dimensions at exactly 35
    - `deriveEmergingType` with scores at 34 — IS emerging. Verify emerging label appears

    **values.test.ts:**
    - `calculateAllValues` with all extreme positive inputs (+3 for every item) — all dimensions should be +100
    - `calculateAllValues` with all extreme negative inputs (-3 for every item) — all dimensions should be -100
    - `calculateAllValues` with mixed extremes — verify no NaN

    **mi.test.ts:**
    - `calculateAllMi` with all-max inputs — all 8 dimensions should be 100
    - `calculateAllMi` with all-min inputs — all 8 dimensions should be 0

    **strengths.test.ts:**
    - `getTopStrengths` with n=0 — should return empty array
    - `accumulateStrengths` with single signal repeated many times — scores accumulate correctly

    **nan-guard.test.ts:**
    - Add boundary tests for each `calculateAll*` function: single-response set (only 1 response) should produce numeric (not NaN) results
  </behavior>
  <action>
    Read each scoring module source to understand exact input formats and expected outputs for boundary values. Then add a `describe("Boundary values", () => { ... })` block to each test file.

    For each test: construct the minimal input that hits the boundary, call the function, assert the output is a specific numeric value (not NaN, not undefined). Use `expect(...).toBe(...)` or `expect(...).toBeCloseTo(...)` for floating point.

    IMPORTANT: Read the actual function signatures and input types before writing tests. The `calculateAll*` functions take `ClientResponse[]` arrays with specific shapes — construct realistic response objects.
  </action>
  <verify>
    <automated>cd "/Users/gerrygan/Career Quest" && npx vitest run lib/scoring/__tests__/ -x 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run lib/scoring/__tests__/` passes with MORE tests than before (was 840 total, scoring subset should increase)
    - grep for "Boundary values" in each test file returns at least 1 match
    - grep for "deriveClassLabel.*50" in riasec.test.ts returns a match
    - grep for "mergeIpsativeScores.*null" in riasec.test.ts returns a match
    - grep for "getTopStrengths.*0" in strengths.test.ts returns a match
    - No NaN assertions anywhere — all boundary outputs are concrete numbers
  </acceptance_criteria>
  <done>Every scoring boundary gap identified in research is covered by an explicit test. All tests pass. Total test count increased by 15+ boundary tests.</done>
</task>

</tasks>

<verification>
After both tasks:
1. `npx eslint app/ components/ hooks/ lib/ --max-warnings 0` exits 0
2. `npx tsc --noEmit` exits 0
3. `npx vitest run` all tests pass, total count > 840
4. No dead code remains (handlePostConfirmatory, unused imports all gone)
</verification>

<success_criteria>
- Zero ESLint warnings and errors in main codebase
- TypeScript compiles clean
- 15+ new boundary-value tests added and passing
- All 840+ existing tests still pass
- Dead code removed: ClassLabel import, handlePostConfirmatory, unused hook imports
</success_criteria>

<output>
After completion, create `.planning/phases/04-quality-audit/04-01-SUMMARY.md`
</output>
